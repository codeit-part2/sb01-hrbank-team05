spring:
  servlet:
    multipart:
      maxFileSize: 10MB
      maxRequestSize: 30MB
  datasource:
    url: jdbc:postgresql://nozomi.proxy.rlwy.net:23506/railway
    username: postgres
    password: cgAuqCnLOZhyFXtsiSBPWqgrTORnxzsP
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database: postgresql

demo:
  storage:
    type: local
    local:
      root-path: C:/storage/binary
