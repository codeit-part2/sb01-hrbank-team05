spring:
  servlet:
    multipart:
      maxFileSize: 10MB
      maxRequestSize: 30MB
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 5

    #url: jdbc:postgresql://nozomi.proxy.rlwy.net:23506/railway
    #username: postgres
    #password: cgAuqCnLOZhyFXtsiSBPWqgrTORnxzsP
    #url: jdbc:postgresql://localhost:5432/hrbank
    #username: hrbank_5
    #password: hrbank_5

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database: postgresql
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Seoul

backup:
  batch:
    interval: 3600000
  directory: ./hrbank05/file/backup

file:
  upload-dir: ./hrbank05/file/backup

demo:
  storage:
    type: local
    local:
      root-path: C:/storage/binary
